Урок 2
Класове. Нива на достъпване. Канонично представяне на клас("голяма четворка"). Get and set методи.

Syntax: 
class _name_
{
private: 
	//fields in the class

(protected:) -> more on protected in the following weeks

public: 
	// big 4
	// methods/functionality
	// getters and setters 
}

Нива на достъпване
public:
private:(this->)

Познато: Конструктор по подразбиране, конструктор с параметри и деструктор. 
Ново: Копи конструктор, оператор за присвояване

Канонично представяне на клас("Голяма четворка")
Конструктор
Деструктор
Копи конструктор
Оператор за присвояване

Задачи:

Задача 1:
Да се напише клас Book, който има име на автор(динамичен масив), 
заглавие(динамичен масив), брой страници, година на издаване.

Класът да има

Голяма четворка
Гетъри
Сетъри - с валидация (не мове годината да е след 2021 или да е отрицателно число)

Задача 2:

Да се напише клас Library, който да има масив от книги и адрес

Класът да има

Голяма четворка
Гетъри
Метод sort_by_bookSize()
Метод sort_alphabetical_order()
Метод за принтиране на масива от книги

Задача 3:

Преправете задачата за масив от миналия път на клас и му напишете голяма четворка.

 
